# render.yaml - FIXED VERSION
services:
  - type: web
    name: det-contact-gain
    env: node
    buildCommand: cd backend && npm install
    startCommand: cd backend && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: EMAIL_USER
        value: gozieezeh2005@gmail.com
      - key: EMAIL_PASS
        value: dvus snub kqck wdbg
      - key: EMAIL_FROM
        value: D.E.T Contact Gain <gozieezeh2005@gmail.com>  # NO QUOTES!
      - key: BATCH_TARGET
        value: 50